# Gateway MCP Notes

MCP integration is marked `example-only` in Sprint F.

The Sprint E gateway is an HTTP gateway. It is not an MCP server. Sprint F can provide an HTTP-resource style example for MCP-based clients, but it does not expose MCP tools or resources.

Future work may add an MCP bridge after the gateway runtime and provider execution model are more mature.
