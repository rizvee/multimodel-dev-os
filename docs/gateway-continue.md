# Gateway Continue Notes

Continue integration is marked `example-only` in Sprint F.

The preview generator can produce local OpenAI-compatible configuration guidance for the mock gateway. It does not install Continue packages and does not edit global configuration files.

Use `mock-chat` for basic chat and `mock-stream` for streaming protocol checks.
