# Registry Security

Registry security controls prevent arbitrary code execution by ensuring:
- All remote catalog and plugin files are declarative configurations only.
- No package installations or script executions can occur from registries or plugins.
- Remote discovery is strictly opt-in.
