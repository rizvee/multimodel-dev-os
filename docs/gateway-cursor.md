# Gateway Cursor Notes

Cursor integration is marked `example-only` in Sprint F.

The preview generator can produce workspace-safe guidance for a localhost OpenAI-compatible endpoint:

- Base URL: `http://127.0.0.1:8787/v1`
- Model: `mock-chat`
- Stream model: `mock-stream`

No user-global Cursor settings are written. Review the generated preview manually before applying it to Cursor.
