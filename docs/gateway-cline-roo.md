# Gateway Cline and Roo Code Notes

Cline and Roo Code integrations are marked `example-only` in Sprint F.

The previews describe OpenAI-compatible provider profile inputs for:

- localhost base URL
- mock model selection
- optional bearer-token placeholder
- streaming notes
- tool-call notes for `mock-tools`

MultiModel Dev OS does not install or execute either extension.
