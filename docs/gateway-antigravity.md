# Gateway Antigravity Notes

Antigravity integration is marked `adapter-ready` in Sprint F because the repository already ships Antigravity adapter assets.

The gateway-specific preview remains local and mock-only. It does not alter Google Cloud credentials, read provider keys, or activate external model calls.

Review the bundled Antigravity adapter files before applying any client-specific configuration manually.
